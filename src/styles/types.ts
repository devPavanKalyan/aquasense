export type LevelGoal = {
  id: string;
  name: string;
};

export type LevelActivity = {
  id: string;
  name: string;
  location: string;
};

export type LevelSensor = {
  id: string;
  name: string;
  slug: string;
};
